with "util";
with "el";
with "asf";
with "testutil";

project Asfunit is
   Version := "@ASF_VERSION@";

   for Library_Name use "asfunit";
   for Library_Dir use "INSTALL/lib";
   for Library_Kind use "LIBRARY_TYPE";

   for Source_Dirs use ("INSTALL/share/ada/adainclude/asfunit");
   for Library_ALI_Dir use "INSTALL/lib/ada/adalib/asfunit/" & Project'Library_Kind;
   for Externally_Built use "true";
end Asfunit;

